"""
Zadanie: Stwórz prosty kalkulator, opierający się na trzech zmiennych a,b i c. Niech a i b będą liczbami a c będzie
operatorem. Następnie przy wywołaniu funkcji określ wartości podane powyżej.
"""
#Tworzymy funkcję.
def calculator(a,b,c):
    #Liczymy ją ifami i returnami.
    if c=="+":
        return a+b
    if c=="-":
        return a-b
    if c=="*":
        return a-b
    if c=="/":
        return a/b
#Drukujemy funkcję, określając jej zmienne, tak jak np. 3,9 i "+". 
print(calculator(3,9,"+"))
