"""
Zadanie: Stwórz kalkulator, opierający się na zmiennych a,b i c. Pierwsze dwie niech będą liczbami, które będą liczone
przez operator - operatorem będzie c. Niech trzy takie zmienne nie będą ustalane w funkcji, ale wpisywane przez użytkownika.
"""
#Tworzymy funkcję.
def calc():
    #Tworzymy trzy zmienne, które może zdefiniować użytkownik.
    a = float(input("Pierwsza liczba:"))
    b = float(input("Druga liczba:"))
    c = input("Operator:")
#Tu sobie liczymy, wystarczą proste ify i return.
    if c=="+":
        return a+b
    if c=="-":
        return a-b
    if c=="*":
        return a*b
    if c=="/":
        return a/b
#Tutaj wywołujemy funkcję.
print(calc())
